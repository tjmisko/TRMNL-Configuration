trmnl.json
.worktrees/
.env
BART/bart
events/sources/ics
events/sources/csc
events/sources/.csc-cache.json
# Private calendar/filter configs (real feed URLs, ignore globs). Auto-ignore
# any events/*.conf so new ones can't be committed by accident; keep examples.
events/*.conf
!events/*.conf.example
